class Solution:
    def sumZero(self, n: int) -> List[int]:
        sum = 0
        list = []
         
        for i in range(1,n):
            list.append(i)
            sum = sum +i
        list.append(-sum)
        return list
        
